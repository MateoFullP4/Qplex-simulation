# MOT Simulation Framework – QPlex

## Overview

This repository contains a modular simulation framework developed during an internship project at QPlex.

It is built on top of the internal physics simulation engine `atomsmltr`, developed by the team.

The objective of this project is to simulate all components of a **Magneto-Optical Trap (MOT)** in order to:

- Study atomic trajectories
- Analyze velocity distributions
- Optimize laser and magnetic field parameters
- Evaluate trapping efficiency
- Improve the experimental setup

The framework was designed to be **fully modular**, ensuring that configurations, simulations, data generation, and plotting remain strictly separated.

---

# Project Structure

```
.
├── configurations/
├── simulations/
├── data/
├── plottings/
├── figures/
```

---

## 1. configurations/

Contains preset physical configurations of the MOT setup.

Each configuration defines:

- Atomic species
- Laser beams (wavelength, waist, power, direction, polarization)
- Magnetic field configuration
- Atom-light couplings (detuning expressed in units of Γ)

Example: `red_mot_configuration.py`

A configuration file typically:

- Instantiates the atom (e.g., Ytterbium)
- Defines laser beams
- Imports a magnetic field configuration
- Adds atom-light couplings
- Exposes a `Configuration` object that can be imported elsewhere

Example usage:

```python
from configurations.red_mot_configuration import configuration_MM
```

### Design Philosophy

Configuration files are **pure physical descriptions**.

They should not:

- Run simulations
- Save data
- Generate plots

This guarantees reproducibility and clean separation between physics definitions and numerical experiments.

---

## 2. simulations/

Contains scripts that:

- Import a configuration
- Define initial atomic conditions
- Run the simulation using `atomsmltr`
- Save results to the `data/` directory

Typical studies include:

- Velocity evolution
- Atomic trajectories
- Capture efficiency
- Parameter sweeps

Each simulation script should:

1. Select a configuration
2. Define initial conditions
3. Run the time evolution
4. Save outputs in a dedicated subfolder inside `data/`

Example execution:

```bash
python simulations/my_simulation.py
```

---

## 3. data/

Stores raw outputs generated by simulations.

Structure example:

```
data/
├── red_mot_velocity_study/
│   ├── parameters.json
│   ├── trajectories.npy
│   ├── velocities.npy
│   └── ...
├── detuning_scan/
│   ├── ...
```

Rules:

- Each simulation has its own subfolder.
- Data should not be manually modified.
- If parameters change, create a new folder instead of overwriting previous results.

This ensures traceability and reproducibility.

---

## 4. plottings/

Contains scripts dedicated to:

- Loading data from `data/`
- Post-processing
- Generating figures
- Saving outputs in `figures/`

No simulation should occur in this folder.

Example execution:

```bash
python plottings/my_plot_script.py
```

---

## 5. figures/

Stores generated plots and visualizations.

Figures are automatically saved by scripts in `plottings/`.

This folder should not contain manually edited files.

---

# How to Run a Simulation

1. Choose or create a configuration in `configurations/`
2. Create or modify a script in `simulations/`
3. Run the simulation:

```bash
python simulations/my_simulation.py
```

4. Results will be saved in:

```
data/my_simulation/
```

5. Generate plots:

```bash
python plottings/my_plot_script.py
```

6. Figures will be saved in:

```
figures/
```

---

# Dependency: atomsmltr

This project relies on the internal physics engine:

```
atomsmltr
```

The engine handles:

- Atomic physics
- Laser-atom interactions
- Magnetic field effects
- Time evolution
- Numerical integration

This repository assumes that `atomsmltr` is already installed and accessible in the Python environment.

---

# Conventions

## Units

All quantities are expressed in SI units:

- Wavelength → meters  
- Power → Watts  
- Distances → meters  
- Time → seconds  
- Detuning → expressed in units of Γ (natural linewidth)  

## Coordinate System

- Origin (0, 0, 0) corresponds to the MOT center.
- Directions are given as normalized 3D vectors.

## Laser Tags

Each laser beam is assigned a unique string tag.

This tag must match the one used in `add_atomlight_coupling()`.

Example:

```python
configuration_MM.add_atomlight_coupling("laser_MM_d1", "main", -1.0 * GAMMA)
```

---

# Design Principles

- Modular architecture  
- Strict separation between configuration, simulation, and plotting  
- Reproducibility of results  
- Parameter traceability  
- Easy extensibility  

---

# For the Next Intern

## To Add a New Configuration

1. Create a new file inside `configurations/`
2. Follow the structure of existing configurations
3. Export a `Configuration` object

---

## To Study a New Parameter

1. Create a new script inside `simulations/`
2. Import an existing configuration
3. Save results in a new folder inside `data/`

Do not overwrite existing data.

---

## To Generate New Figures

1. Create a script inside `plottings/`
2. Load the required dataset from `data/`
3. Save figures inside `figures/`

---

## To Modify atomsmltr

Proceed carefully.

Changes in the engine may affect all simulations and previously validated results.

---

# Possible Improvements

- Automated parameter sweep tools  
- Metadata versioning system  
- Logging framework  
- Command-line interface  
- Unit tests  
- Automatic data integrity checks  
- Documentation of `atomsmltr` internals  

---

# Final Notes

This repository was developed to provide a flexible and extensible framework for MOT optimization studies.

The modular structure is intentional and should be preserved to maintain clarity, reproducibility, and scalability of future work.
